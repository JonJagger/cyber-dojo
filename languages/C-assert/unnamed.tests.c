#include "unnamed.h"
#include <assert.h>
#include <stdio.h>

// The initial source files for the chosen language
// and test library are always the same and have no
// relationship to the chosen exercise.

static void example(void)
{
    assert(hhg() == 6*9);
}

int main(void)
{
    example();
    puts("All tests passed");
}
