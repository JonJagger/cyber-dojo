
<script type="text/javascript"><!--

$(document).ready(function() {

  //cd.tipify($('.tipped'), $('#tip-window'));

  if ("<%=@avatar_name%>" !== "") {
    var id = "<%=@id%>";
    var avatarName = "<%=@avatar_name%>";
    var wasTag = parseInt("<%=@was_tag%>");
    var nowTag = parseInt("<%=@now_tag%>");
    var maxTag = parseInt("<%=@max_tag%>");
    cd.dialog_diff(id, avatarName, wasTag, nowTag, maxTag);
  }

});

//--></script>

<div id="dashboard-page">
  <table>
    <tr align='right'>
      <td class="valign-top">
        <div>
          <%= render :partial => 'shared/info' %>
        </div>
        <div>
          <%= render :partial => 'controls' %>
        </div>
        <div id="age">
          <%= render :partial => 'age' %>
        </div>
        <div id="team-bulb-counts">
          <%= render :partial => 'team_bulb_counts' %>
        </div>
        <div>
          <%= render :partial => 'help_button' %>
        </div>
      </td>
      <td class="valign-top" rowspan="2">
        <div id="traffic-lights">
          <%= render :partial => 'traffic_lights' %>
        </div>
      </td>
    </tr>
  </table>
</div>
