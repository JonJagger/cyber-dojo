
<script type="text/javascript"><!--

$(document).ready(function() {

  $('#help_button').click(function() {
    var title = $(this).text();
    var ok = $(this).data('ok');
    $.get('/dashboard/help_dialog', function(html) {
      cd.dialog(html, title, ok).dialog('open');
    });
  });

});

//--></script>

<button type="button"
        value="Show help"
        data-ok="<%= t('views.buttons.ok') %>"
       id="help_button">
  ?
</button>

