
<script language="javascript" type="text/javascript"><!--

$j(document).ready(function() {
  $cd.buildDiffFilenames(<%= @diffs.to_json.html_safe -%>);
  $j('#' + '<%=@current_filename_id-%>').click();
  
  $j('#start_a_kata_from_here').click(function() {
      $cd.postTo('/dojo/diff_save', {
        id: "<%= @kata.id -%>",
        language: "<%= @kata.language.name -%>",
        exercise: "<%= @kata.exercise.name -%>",
        avatar: "<%= @avatar.name -%>",
        tag: "<%= @traffic_lights_to_tag.length -%>"
      }, '_blank');
    });
  $j('input[type=radio]').hide();
});

//--></script>

<table>
  <tr valign="top">
    <td>
      <div class="panel">
        <%= render :partial => 'info' -%>
      </div> 
      <div class="panel">
        <%= render :partial => 'prev_next' -%>
      </div>      
      <div class="panel">
        <%= render :partial => 'filenames' -%>
      </div>
      <div class="info panel">
        <br/>
        <%= render :partial => 'shared/info' -%>
        <br/>
          <table><tr><td>
            <div id="start_a_kata_from_here"
                  class="button">
              start a new<br/>
              practice-kata<br/>
              from here
            </div>
            </td></tr></table>
      </div>
    </td>    
    <td>
      <%= render :partial => 'view' -%>
    </td>
  </tr>
</table>

