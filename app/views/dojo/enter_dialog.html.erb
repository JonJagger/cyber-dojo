
<div>
  <div class="align-center small">
    <div><%= t("views.labels.your_animal_is") %></div>
    <div><%= t("views.avatars.#{@avatar_name}") %></div>
    <% size = 120 %>
    <img class='avatar_image'
         src="/images/avatars/<%= @avatar_name %>.jpg"
         title="<%= @avatar_name.humanize %>"
         width="<%= size %>"
         height="<%= size %>" />
  </div>
</div>
