
<button type="button"
        id="test-button"
        title="runs the tests and generates a new traffic-light"
        value="<%= t('views.buttons.test') %>">
  <%= t('views.buttons.test') %>
</button>
