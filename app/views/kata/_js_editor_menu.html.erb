
<script language="javascript" 
        type="text/javascript">

document.observe('dom:loaded', function() 
{
  setupEditorMenu();
  // TODO: load file that was in editor on this increment
  // TODO: set its caretPos
  loadFile('kata.sh');
})

function setupEditorMenu()
{
  var editorMenuItems = [
    {
      name: 'Run the tests  ',
      className: 'run-the-tests', 
      callback: function(e) { runTests(); }
    }
  ]

  new Proto.Menu({
    selector: '#editor', 
    className: 'menu desktop',
    menuItems: editorMenuItems
  })
}

</script>

