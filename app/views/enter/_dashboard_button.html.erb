
<script type="text/javascript"><!--

$(function() {

  var button = $('#dashboard-button');

  button.click(function() {
    var id = $('#kata-id-input').val();
    var cursor = button.css('cursor');
    button.css('cursor', 'wait');
    $.getJSON('/enter/continue', { id: id }, function(dojo) {
      button.css('cursor', cursor);
      if (dojo.empty) {
        cd.continueDialog('dashboard', dojo.html).dialog('open');
      } else {
        window.location.href = cd.homePageUrl(id);
        window.open('/dashboard/show/' + id);
      }
    });
  });

});

//--></script>

<button type="button" id="dashboard-button">dashboard</button>
